### C YAML Parser

This is a C project for parsing YAML files with libyaml. It includes testing with unity. Dependency management is handled with CMake.

``` bash
./compile # Compiles with CMake
./test # Runs tests with unity
./check # Compiles and runs tests
./parse test.yaml # Parses test.yaml file in /configs
```